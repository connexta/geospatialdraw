import { GeometryJSON, LengthUnit } from '../geometry';
declare const updateCircleGeo: (geo: GeometryJSON, lat: number, lon: number, radius: number, radiusUnit: LengthUnit) => GeometryJSON;
export { updateCircleGeo };
